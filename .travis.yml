language: node_js
node_js:
  - "6.9"

branches:
  only:
  - master
  - stable

before_script:
  - npm install -g firebase-tools

script:
  - npm run build

after_success:
  - firebase deploy --token $FIREBASE_TOKEN